import React from 'react';

const Home = (props) => {
    return (
        <div className="Button">
            <button>Lets PLay some music</button>
        </div>
        
    );
}

export default Home;